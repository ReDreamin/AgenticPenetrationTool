一．背景简介/问题描述

1.1实验介绍
（主要包含问题描述、问题产生的背景、研究现状分析、拟采用的方法等）

1.2实验准备
（主要包含实验环境介绍）

1.3实验详细设计与实现
1.3.1数据准备
（主要包含实验数据的收集、存储格式、目录结构和预处理介绍）

1.3.2实验步骤
（主要包含实验流程介绍【如何设置训练集、测试集、是否需要交叉验证等等】、性能指标介绍、对比方法介绍【可选，也可以和自己改进前进行对比】）

二．算法介绍
（主要包含算法思路、流程介绍，建议配合模型结构图或者算法流程图）



三．算法实现
3.1 核心算法实现
（包括核心功能代码实现（截图或者用表格），请做好代码注释工作）
例如：
代码片段X. 一个代码例子。代码部分5号字体，中文宋体，英文Time New Roman 
#定义模型结构，MindSpore中的模型时通过construct定义模型结构，在__init__中初始化各层的对象
class LeNet5(nn.Cell):
    def __init__(self, num_class=10, num_channel=1):
        super(LeNet5, self).__init__()
#定义卷积层，ReLU激活函数，平坦层和全连接层
        #conv2d的输入通道为1维，输出为6维，卷积核尺寸为5*5，步长为1，不适用padding
        self.conv1 = nn.Conv2d(num_channel, 6, 5, pad_mode='valid')
        self.conv2 = nn.Conv2d(6, 16, 5, pad_mode='valid')
        self.fc1 = nn.Dense(16 * 5 * 5, 120, weight_init=Normal(0.02))
        self.fc2 = nn.Dense(120, 84, weight_init=Normal(0.02))
        self.fc3 = nn.Dense(84, num_class, weight_init=Normal(0.02))
        self.relu = nn.ReLU()
        self.max_pool2d = nn.MaxPool2d(kernel_size=2, stride=2)
        self.flatten = nn.Flatten()
    #构建Lenet5架构，x代表网络的输入
    def construct(self, x):
        x = self.max_pool2d(self.relu(self.conv1(x)))
        x = self.max_pool2d(self.relu(self.conv2(x)))
        x = self.flatten(x)
        x = self.relu(self.fc1(x))
        x = self.relu(self.fc2(x))
        x = self.fc3(x)
        return x

3.2 算法实验结果展示
（包括实验指标性结果展示、实验效果图展示、与对比算法的指标表格对比与分析等）

四．讨论及结论

1．优缺点分析

2．总结与思考


五．参考文献
[1]使用自定义算法构建模型（手写数字识别）_AI开发平台ModelArts_最佳实践_模型训练（自定义算法-新版训练）_华为云 (huaweicloud.com) https://support.huaweicloud.com/bestpractice-modelarts/modelarts_10_0080.html
[2] 

注：
1. 报告中图的下方要有图题（如图1. XXXX），表格需要用三线表，表头需列在表的上方（如表1. XXXX）。图表居中排列。
2. 提交时请修改文件名为：“学号-姓名-题目.docx”